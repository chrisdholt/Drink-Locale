$(document).ready(function() {

  var beer, beerId;

  //Parse the URL to get the user's input
  function getUserInput(inputName) {
    var urlSearch = window.location.search;
    var inputArray = urlSearch.split(inputName + "=");
    return inputArray[1];
  };


  var beer =

});
